## Rules
- After completing a task, always update CHANGELOG.md in WhatsApp style (using emojis, bold headings, and clean bullet points for all versions), increment the build version in pubspec.yaml, build the debug/release APK, and push the code changes to GitHub.
